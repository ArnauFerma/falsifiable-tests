---
max_turns: 40
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Bash, Edit, Write, Skill]
---

Add a pytest test for `apply_coupon` in `orders.py`, in `tests/test_orders.py`. Check that a 10% coupon on a 120.00 subtotal gives 108.00. Run the tests with `.venv/bin/python -m pytest -q` when you're done.
