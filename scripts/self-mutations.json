[
  {"name": "error lumped into fail", "file": "scripts/mutate.py",
   "find": "            per_test[key] = ERROR", "replace": "            per_test[key] = FAIL"},
  {"name": "failure classified as pass", "file": "scripts/mutate.py",
   "find": "        elif \"failure\" in tags:\n            per_test[key] = FAIL", "replace": "        elif \"failure\" in tags:\n            per_test[key] = PASS"},
  {"name": "absent treated as pass", "file": "scripts/mutate.py",
   "find": "run.per_test.get(t, ABSENT)", "replace": "run.per_test.get(t, PASS)"},
  {"name": "exit 127 treated as red", "file": "scripts/mutate.py",
   "find": "if self.returncode in (126, 127):", "replace": "if self.returncode in (126,):"},
  {"name": "baseline output tail dropped", "file": "scripts/mutate.py",
   "find": "        print(baseline.tail())\n        return 1\n    baseline_tests", "replace": "        return 1\n    baseline_tests"},
  {"name": "bytecode guard removed", "file": "scripts/mutate.py",
   "find": "PYTHONDONTWRITEBYTECODE=\"1\"", "replace": "PYTHONDONTWRITEBYTECODE=\"\""},
  {"name": "skips counted as tests", "file": "scripts/mutate.py",
   "find": "if v != SKIP}", "replace": "if True}"},
  {"name": "final run result ignored", "file": "scripts/mutate.py",
   "find": "final_ok = final.command_ran and final.suite_passed", "replace": "final_ok = True"},
  {"name": "restore skipped after each mutation", "file": "scripts/mutate.py",
   "find": "            finally:\n                ws.restore_all()\n\n            outcomes", "replace": "            finally:\n                pass\n\n            outcomes"},
  {"name": "ambiguous find accepted", "file": "scripts/mutate.py",
   "find": "elif count > 1:", "replace": "elif count > 99:"}
]
